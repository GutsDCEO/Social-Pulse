# ================================================
# social-pulse — Docker Compose
# Generated by Project Maker
# ================================================

services:
# --- Backend (Monolith) ---
backend:
build: ./backend
ports:
- "8080:8080"
environment:
DB_HOST: postgres
DB_PORT: 5432
DB_NAME: socialpulse_db
DB_USER: postgres
DB_PASSWORD: changeme
JWT_SECRET: $\{JWT_SECRET:-change-this-in-production}
depends_on:
postgres:
condition: service_healthy

# --- Database ---
postgres:
image: postgres:16-alpine
ports:
- "5432:5432"
environment:
POSTGRES_USER: postgres
POSTGRES_PASSWORD: changeme
POSTGRES_DB: socialpulse_db
volumes:
- postgres_data:/var/lib/postgresql/data
healthcheck:
test: ["CMD-SHELL", "pg_isready -U postgres"]
interval: 10s
timeout: 5s
retries: 5



volumes:
postgres_data:
